import processing.core._
import SlapEmAll._
import java.io._
import javax.sound.sampled._
import javax.swing._

object SoundPlayer{
  
  val pirate = new Sound("piraattilaulu.wav")
  /*
  val soundFile = new File("piraattilaulu.wav");
  val audioIn = AudioSystem.getAudioInputStream(soundFile)
  var clip = AudioSystem.getClip()
  clip.open(audioIn)
  clip.start()
  */
  playSound()
  /*
  var muted = false
  
  def mute() = {
    
  }
  
  def unmute() = {
    muted = false
  }
  */
 def playSound() = {
   while(true){ 
     pirate.play
   }
 }
}
